export type LandingSectionProps = {
    canRegister: boolean;
};

export type LandingShowcaseProduct = {
    id: string;
    name: string;
    subtitle: string;
    priceLabel: string;
    imageUrl: string;
    badge?: string;
};
